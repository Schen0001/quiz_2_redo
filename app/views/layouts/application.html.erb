<!DOCTYPE html>
<html>
  <head>
    <title>Quiz 2 Redo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>

    <div class="navbar">

      <a href="http://www.localhost:3000/" class="logo"><%= image_tag('logo.jpg', width: '100px' , height: '40px') %></a>

    <span style="padding: 0px 5px 0px 0px;">
      <%= link_to "Ideas Index Page" , posts_path %>
    </span>

      <% if user_signed_in? %>
        Hello Mr./Mrs. <%= current_user.last_name %>,
          <%= link_to "Logout" , sessions_path, method: :delete, data: {confirm: "are you sure you want to log out?" }%>
            <% else %>
              <%= link_to "Login" , new_sessions_path %> |
                <%= link_to "Sign Up" , new_user_path %> 
                  <% end %>
  </div>
  <span style="display: flex; justify-content: flex-end; padding: 10px;">
    <%= link_to "New Idea" , new_post_path, :class=> "btn" %>
  </span>
    <hr>

    <%= yield %>
  </body>
</html>
